# Required
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Optional
POLL_INTERVAL=10        # seconds between Kraken price polls (default: 10)
DB_PATH=bot.db          # SQLite database file path (default: bot.db)
LOG_LEVEL=INFO          # logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
